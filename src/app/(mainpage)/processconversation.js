import { useState } from "react";

export const useProcessConversation = () => {

    return [convoArr, handleConvo, handleHistory, handleNewConversation, handleDeleteConverSation, handleFetchHistory, handleUpdateScreen]

}
